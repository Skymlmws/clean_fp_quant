# Project runners

These shell entry points launch method-independent profiling and visualization
tools owned by `video_quant_lab`. They resolve the repository root relative to
their own location, so moving or cloning the project does not require editing a
hard-coded path.

Baseline-specific launchers do not belong here. They remain with the baseline
repository or are described by a command manifest.
