# Video Quantization Lab tests

These tests cover the method-neutral harness, activation analysis, artifact
rendering, and visualization helpers. They must not import a baseline's
quantizer or transform implementation.

Run them from the repository root:

```shell
python -m pytest -q video_quant_lab/tests
```
