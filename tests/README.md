# FP-Quant baseline extension tests

This directory now contains tests that directly exercise the local FP-Quant
baseline fork: the Givens transform, Wan RTN integration, and FP-Quant weight
quantization statistics.

Run them from the repository root:

```shell
python -m pytest -q tests
```
